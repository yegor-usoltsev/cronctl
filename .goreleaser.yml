---
version: 2

project_name: cronctl

before:
  hooks:
    - go mod tidy

builds:
  - env:
      - CGO_ENABLED=0
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_386
      - linux_amd64
      - linux_arm
      - linux_arm64
    flags:
      - -trimpath
    ldflags:
      - -s -w -X github.com/yegor-usoltsev/cronctl/internal/version.Version=v{{ .Version }}

changelog:
  sort: asc
  filters:
    exclude:
      - "^Merge "

checksum:
  name_template: checksums.sha256.txt
  algorithm: sha256

report_sizes: true

release:
  github:
    owner: yegor-usoltsev
    name: cronctl
  name_template: "v{{ .Version }}"
